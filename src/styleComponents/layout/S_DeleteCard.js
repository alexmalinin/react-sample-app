import styled from 'styled-components';
import { Icon } from 'semantic-ui-react'

export const S_DeleteCard = styled(Icon)`  
      position: absolute;
      right: 0px;
      top: -8px;
      padding: 20px;
      cursor: pointer;
`;
